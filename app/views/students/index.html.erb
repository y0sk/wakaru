<% @students.any? %>
    <% @students.each do |student| %>
        <div>
            <div>
                <%= student.name %>
            </div>
            <div>
                <p><%= link_to '生徒の詳細', student_path(student) %></p>
            </div>
        </div>
    <% end %>
<% end %>
        